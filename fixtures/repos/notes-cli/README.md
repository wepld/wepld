# notes-cli

A tiny fixture repository used by WePLD's M0 demo and golden trace. It is a
plain source tree (no `.git`); the demo copies it to a scratch directory and
runs `git init` there so nothing nests inside the WePLD repository.
